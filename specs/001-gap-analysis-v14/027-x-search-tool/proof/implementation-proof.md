# 027 x_search — Implementation Proof

Shipped:

- `crates/edgecrab-tools/src/tools/x_search.rs`
- Opt-in toolset `x_search`

Auth: `XAI_API_KEY` or `~/.edgecrab/auth.json` providers (`xai` / `grok`).
