# 001 — Peer Field (July 2026)

Code is law for EdgeCrab. Peer facts from public releases / docs as of 2026-07.

## Hermes Agent (Nous Research)

- Releases: v0.18.0 “Judgment” (v2026.7.1) through v2026.7.7.2
- **Steal:** `/goal` completion contracts (`outcome/verification/constraints/boundaries/stop_when`); coding verification evidence ledger; `pre_verify` hook
- **Reject:** MoA as core product; global `allow_private_urls`; `hard_stop_enabled` default **false**
- Local checkout: `/Users/raphaelmansuy/Github/03-working/hermes-agent` (`agent/tool_guardrails.py`, `agent/turn_finalizer.py`, `agent/verification_evidence.py`)

## Pi (pi.dev / Earendil)

- Minimal 4-tool core (`read`/`write`/`edit`/`bash`); skills progressive disclosure (metadata in prompt, body via `read`)
- Compaction + `/tree` session DAG; `PI_CACHE_RETENTION`; packages/extensions
- **Steal:** message-path progressive load; keep hot wire small
- **Reject:** extension-everything as P0; Node package economy; session-tree as blocking work

## Claude Code

- ~30 lifecycle hook events; Pre/PostToolUse block via exit codes; Stop / TaskCompleted enforcement
- **Steal:** blockable PreToolUse at existing `lifecycle_hooks` + `guardrail_before_dispatch`; thin `PreVerify` emit
- **Reject:** cloning 30-event surface; mutating system prompt from hooks

## EdgeCrab positioning

SOTA in integrity + Anthropic-safe cache + indexed tools + armed guardrails + port-scoped preview + single binary + gateway. Not global SOTA on public TB scores, Pi package culture, or Hermes backends.
