name: Release — Python (PyPI)

on:
  push:
    tags: ['v[0-9]+.[0-9]+.[0-9]+']
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag name to publish (e.g. v0.3.3)'
        required: true
        default: 'v0.3.3'

permissions:
  id-token: write
  contents: read

jobs:
  build:
    name: Build sdist + wheel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.ref || (inputs.tag_name || github.ref) }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Resolve version from tag
        id: version
        env:
          INPUT_TAG_NAME: ${{ inputs.tag_name }}
        run: |
          TAG="${INPUT_TAG_NAME:-$GITHUB_REF_NAME}"
          VERSION="${TAG#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Publishing edgecrab==$VERSION"

      - name: Sync release version
        run: |
          ./scripts/release-version.sh set "${{ steps.version.outputs.version }}"
          ./scripts/release-version.sh check

      - run: pip install --upgrade build maturin

      - name: Build distributables
        run: |
          cd sdks/python
          python -m build --sdist --outdir dist/
          maturin build --release --out dist/ -i python3.10 || true

      - name: Drop non-PyPI-compatible Linux wheels
        run: |
          python3 - <<'PY'
          import pathlib
          dist = pathlib.Path('sdks/python/dist')
          for wheel in dist.glob('*.whl'):
              name = wheel.name
              if 'linux_' in name and 'manylinux' not in name and 'musllinux' not in name:
                  print(f'Removing unsupported wheel: {name}')
                  wheel.unlink()
          PY

      - uses: actions/upload-artifact@v4
        with:
          name: python-sdk-dist
          path: sdks/python/dist/
          if-no-files-found: error

  publish-pypi:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: python-sdk-dist
          path: dist/

      - name: List dist contents
        run: ls -lh dist/

      - name: Publish to PyPI (OIDC preferred, token fallback)
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          skip-existing: true
          # PYPI_API_TOKEN is used when the project is new (OIDC trusted publisher
          # must be registered for existing projects, but works without for token).
          # Once edgecrab is registered as a trusted publisher on pypi.org, this
          # password: line can be removed.
          password: ${{ secrets.PYPI_API_TOKEN }}
