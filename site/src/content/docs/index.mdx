---
title: EdgeCrab — Your Super Powerful Personal Assistant
description: EdgeCrab is a SuperAgent for personal assistance and coding, built as a native Rust binary with a ratatui TUI, ReAct tool loop, multi-provider LLM support, and messaging gateways. Install via npm, pip, cargo, Docker, or native binaries.
template: splash
hero:
  title: |
    Not a Coding Agent. Your <span class="font-black text-transparent bg-clip-text bg-gradient-to-b from-orange-600 to-red-600">Personal Assistant</span>, Built in Rust.
  tagline: |
    The user-alignment soul of Nous Hermes. The always-on presence of OpenClaw. Native Rust delivery for your laptop, your server, or the edge.
  actions:
    - text: Get Started
      link: /getting-started/quick-start/
      icon: right-arrow
    - text: Star on GitHub
      link: https://github.com/raphaelmansuy/edgecrab
      icon: external
      variant: minimal
---

import FeatureGrid from '../../components/landing/FeatureGrid.astro';
import QuickStart from '../../components/landing/QuickStart.astro';
import CtaSection from '../../components/landing/CtaSection.astro';
import ComparisonSection from '../../components/landing/ComparisonSection.astro';
import BenchmarkSection from '../../components/landing/BenchmarkSection.astro';
import WhyRustSection from '../../components/landing/WhyRustSection.astro';
import UseCasesSection from '../../components/landing/UseCasesSection.astro';
import { coreToolCount, gatewayCount, providerCount, workspaceVersion } from '../../data/project-facts';

<FeatureGrid
  title="Two Great Traditions. One Rust Binary."
  subtitle={`EdgeCrab synthesises Hermes Agent's ethos of autonomous learning, persistent memory, and user-first alignment with OpenClaw's vision of an always-on assistant living in your channels. Delivered as a native Rust binary: ${coreToolCount} core tools, ${providerCount} LLM providers, ${gatewayCount} messaging gateways, and no Python or Node runtime requirement for the agent itself.`}
  features={[
    {
      icon: 'terminal',
      title: 'ratatui TUI — 60 fps Capable',
      description: 'Full-screen terminal UI with streaming output, ghost-text completion, fuzzy slash-command search, multi-line input, approval dialogs, and mouse scroll. Built with ratatui — GPU-composited, silky smooth.',
    },
    {
      icon: 'refresh',
      title: 'ReAct Tool Loop',
      description: `Autonomous Reason → Act → Observe loop runs up to 90 iterations. Dispatches ${coreToolCount} registered core tools, including semantic LSP operations, fires learning reflection after ≥5 tool calls, and auto-compresses context at 50% threshold.`,
    },
    {
      icon: 'globe',
      title: `${providerCount} LLM Providers`,
      description: 'GitHub Copilot, OpenAI, Anthropic, Google Gemini, xAI Grok, DeepSeek, Mistral, Groq, HuggingFace, Z.AI, OpenRouter, Ollama, LM Studio, VertexAI, and AWS Bedrock — hot-swappable mid-session via /model.',
    },
    {
      icon: 'layers',
      title: `${gatewayCount} Messaging Gateways`,
      description: 'Telegram, Discord, Slack, WhatsApp, Signal, Matrix, Mattermost, DingTalk, SMS, Email, Home Assistant, Webhook, API Server, Feishu/Lark, WeCom — streaming delivery with 300ms edit intervals.',
    },
    {
      icon: 'cpu',
      title: 'Sub-agent Delegation',
      description: 'Fork up to 3 parallel sub-agents for complex tasks. Each child inherits provider + tool registry but has its own session state, budget, and cancellation token. Max depth: 2 levels.',
    },
    {
      icon: 'box',
      title: 'Sandboxed Code Execution',
      description: 'Run Python, JS, Bash, Ruby, Perl, or Rust in an isolated subprocess. Scripts can call 7 tools via Unix socket RPC. API keys stripped. 5-min timeout. 50 KB output cap.',
    },
    {
      icon: 'zap',
      title: 'Mixture of Agents',
      description: 'Run a single prompt through Claude Opus 4.6, Gemini 2.5 Pro, GPT-4.1, and DeepSeek R1 in parallel, then synthesize a consensus answer. Maximum intelligence for hard problems.',
    },
    {
      icon: 'shield',
      title: 'Compiled-in Security',
      description: '7 independent security layers: path jail (SanitizedPath type), SSRF guard (SafeUrl type), command scan, prompt injection detection, code sandbox env stripping, skills threat scanner, output redaction.',
    },
    {
      icon: 'terminal',
      title: '6 Terminal Backends',
      description: 'Plug in Local (default), Docker container, SSH ControlMaster, Modal cloud sandbox, Daytona cloud dev box, or Singularity/Apptainer HPC overlay. Switch with one config line.',
    },
    {
      icon: 'refresh',
      title: 'Persistent Memory & Learning',
      description: 'Three-layer memory: MEMORY.md files, SQLite FTS5 session history (instant full-text search), and cross-session Honcho user model. Auto-learning reflection fires after ≥5 tool calls.',
    },
    {
      icon: 'layers',
      title: 'Skills Library',
      description: 'Reusable agent procedures as markdown files. Browse the public hub, install in one command, preload per session with -S. The agent creates new skills autonomously during learning reflection.',
    },
    {
      icon: 'globe',
      title: 'Full MCP Client',
      description: 'Built-in Model Context Protocol client — connect any MCP server and its tools appear in the registry automatically. Hot-reload with /reload-mcp. Stdio, SSE, and HTTP transports.',
    },
    {
      icon: 'cpu',
      title: 'Cron Scheduling',
      description: 'Schedule recurring tasks with standard cron syntax. The agent can schedule its own follow-ups. Deliver proactive summaries to your Telegram at 9am every weekday.',
    },
    {
      icon: 'box',
      title: 'Checkpoints & Rollback',
      description: 'Filesystem snapshots before every destructive operation. Restore any checkpoint with /rollback. Up to 50 snapshots per session. Never lose work from an agent mistake again.',
    },
    {
      icon: 'zap',
      title: 'Profiles & Worktrees',
      description: 'Switch contexts with edgecrab profile use work/homelab/research. Isolate risky experiments in git worktrees with -w. Each profile has its own keys, model, memory, and toolsets.',
    },
    {
      icon: 'shield',
      title: 'ACP / VS Code Copilot',
      description: 'Built-in JSON-RPC 2.0 stdio adapter for Agent Communication Protocol. Registers as a VS Code Copilot agent, Zed agent, or any ACP-compatible runner. No plugin required.',
    },
  ]}
/>

<WhyRustSection
  eyebrow="The Origin Story"
  title="Same Soul. Same Dream. 100× Leaner."
  subtitle="EdgeCrab doesn't replace hermes-agent or OpenClaw — it distils the best of both into a single Rust binary that removes the Python and Node runtime stack from the deployment story."
  panels={[
    {
      tag: 'vs. hermes-agent',
      icon: 'brain',
      headline: 'Same soul, 50× lighter',
      body: 'hermes-agent is brilliant — autonomous learning, persistent memory, Honcho user model, deep tool chains. It inspired EdgeCrab\'s entire architecture. The difference: hermes-agent ships through a Python runtime and uv-managed environment. EdgeCrab ships as compiled Rust: same memory system, same Honcho integration, same learning reflection — in one native executable.',
      wins: [
        'No Python runtime or uv bootstrap in the deployment path',
        'Single native binary with no Python or Node runtime dependency for the agent itself',
        'One executable to copy, version, and verify',
        '7 compiled-in security layers vs runtime patches',
      ],
      honest: 'hermes-agent has a richer Python ecosystem for data-science workflows. If you live in pandas / NumPy and need native Python interop in your agent tools, hermes-agent still wins that specific niche.',
    },
    {
      tag: 'vs. OpenClaw',
      icon: 'bolt',
      headline: 'Same reach, 100× leaner',
      body: 'OpenClaw nailed the vision: an always-on personal assistant living in your channels. EdgeCrab took that vision and compressed it into a single native executable instead of a Node.js runtime stack.',
      wins: [
        `${gatewayCount} gateways with streaming delivery vs OpenClaw's excellent 24+ adapters`,
        'Native binary deployment instead of a Node.js runtime stack',
        'Built-in cron scheduler — no external crontab wrangling',
        'ReAct autonomous loop vs single-turn responses',
      ],
      honest: `OpenClaw has 24+ channel adapters — more breadth than EdgeCrab's ${gatewayCount} gateways today. If you need SMS shortcodes or IRC bots out of the box, OpenClaw still leads there.`,
    },
    {
      tag: 'the synthesis',
      icon: 'merge',
      headline: 'One binary, both dream',
      body: 'EdgeCrab is what you get when you take Hermes Agent\'s soul (autonomous learning, memory, user alignment, Honcho) and pair it with OpenClaw\'s vision (always-on channels, instant presence) — then forge the result in Rust. You keep 100% of the power, you shed 95% of the weight. No Python venv, no Node.js runtime, no 500 MB node_modules. Just one binary that runs your agent anywhere.',
      wins: [
        `${coreToolCount} core tools + ${gatewayCount} gateways + memory + learning — in one download`,
        'Sub-agent delegation up to 3 parallel children, depth 2',
        'Mixture of Agents: 4 frontier models in parallel, synthesized',
        '6 terminal backends: local, Docker, SSH, Modal, Daytona, Singularity',
      ],
      honest: 'It\'s newer. hermes-agent and OpenClaw have years of battle-testing and community plugins. If stability in a mature ecosystem is your #1 requirement, that counts for something real.',
    },
  ]}
/>

<BenchmarkSection
  eyebrow="By the Numbers"
  title="Verified Project Facts"
  subtitle="Every number below is source-derived from the current tree, except where explicitly marked as a local measurement."
  metrics={[
    {
      label: 'Core Tools',
      unit: 'count (source-derived)',
      lowerIsBetter: false,
      bars: [
        { name: 'EdgeCrab', value: coreToolCount, max: 100, formatted: `${coreToolCount} core`, win: true },
      ],
      note: 'Derived from `CORE_TOOLS` in `crates/edgecrab-tools/src/toolsets.rs`.',
    },
    {
      label: 'LLM Providers',
      unit: 'count (source-derived)',
      lowerIsBetter: false,
      bars: [
        { name: 'EdgeCrab', value: providerCount, max: providerCount, formatted: `${providerCount} built-in`, win: true },
      ],
      note: 'Derived from `providers:` in `crates/edgecrab-core/src/model_catalog_default.yaml`.',
    },
    {
      label: 'Gateway Adapters',
      unit: 'count (source-derived)',
      lowerIsBetter: false,
      bars: [
        { name: 'EdgeCrab', value: gatewayCount, max: gatewayCount, formatted: `${gatewayCount} adapters`, win: true },
      ],
      note: 'Derived from the gateway modules exported in `crates/edgecrab-gateway/src/lib.rs`.',
    },
  ]}
  caveatText={`<strong>Release note:</strong> the current workspace version is v${workspaceVersion}. Source-derived counts come directly from the current repository state.`}
/>

<UseCasesSection
  eyebrow="What You'll Actually Do"
  title="Your SuperAgent in the Wild"
  subtitle="EdgeCrab isn't a demo — it's daily infrastructure. These are the real workflows it enables, from a Telegram DM to a full autonomous coding session."
  cases={[
    {
      emoji: '📱',
      tag: 'Telegram + Cron',
      tagColor: 'orange',
      title: 'Morning standup summaries, automatically',
      scenario: 'Every weekday at 9 am, EdgeCrab tails your git log, reads your open PRs, and sends a clean standup summary to your personal Telegram — without you lifting a finger.',
      steps: [
        { icon: '⏰', text: 'edgecrab cron add "0 9 * * 1-5" "morning standup summary"' },
        { icon: '🔍', text: 'Agent reads git log + open GitHub PRs via tool calls' },
        { icon: '📤', text: 'Formats and sends via Telegram gateway' },
      ],
    },
    {
      emoji: '🎙️',
      tag: 'WhatsApp + Audio',
      tagColor: 'green',
      title: 'Voice message → code review',
      scenario: 'Send a WhatsApp voice note describing a bug. EdgeCrab transcribes it with Whisper, locates the relevant files, runs the tests, and replies with a diagnosis and diff — all before you finish your coffee.',
      steps: [
        { icon: '🎤', text: 'Receive WhatsApp voice message via gateway' },
        { icon: '📝', text: 'transcribe_audio tool calls Whisper to get text' },
        { icon: '🔧', text: 'ReAct loop reads code, runs cargo test, produces diff' },
        { icon: '💬', text: 'Sends diagnosis + patch back to WhatsApp thread' },
      ],
    },
    {
      emoji: '🚀',
      tag: 'CLI + Git Worktree',
      tagColor: 'blue',
      title: 'Risky refactor? Isolated worktree',
      scenario: 'Spin up an isolated git worktree for any experimental change. The agent works in a branch-isolated copy — if it goes sideways, just delete the worktree and nothing touches main.',
      steps: [
        { icon: '🌿', text: 'edgecrab -w "refactor auth module to use sessions"' },
        { icon: '🔀', text: 'Creates isolated git worktree automatically' },
        { icon: '🤖', text: 'Agent reads, edits, and tests inside worktree only' },
        { icon: '✅', text: 'Review diff, merge to main when satisfied' },
      ],
    },
    {
      emoji: '🤖',
      tag: 'Delegation + MoA',
      tagColor: 'purple',
      title: 'Hard problem? Throw 4 frontier models at it',
      scenario: 'Send the agent a genuinely hard architectural question. Mixture of Agents spawns Claude Opus 4.6, Gemini 2.5 Pro, GPT-4.1, and DeepSeek R1 in parallel — each reasons independently, then Claude aggregates the best answer.',
      steps: [
        { icon: '🧠', text: 'edgecrab "Should we use event sourcing or CQRS for this?"' },
        { icon: '⚡', text: 'MoA spawns 4 frontier model calls in parallel' },
        { icon: '🔀', text: 'Claude Opus 4.6 aggregates reasoning into synthesis' },
        { icon: '📋', text: 'Returns consensus answer with dissenting notes' },
      ],
    },
    {
      emoji: '🏠',
      tag: 'Home Assistant',
      tagColor: 'green',
      title: 'Smart home, natural language',
      scenario: 'Ask EdgeCrab to "turn off everything that\'s been on for over 4 hours and set bedtime mode." It queries HA entity states, applies the logic, calls services, and confirms what it changed.',
      steps: [
        { icon: '🔍', text: 'ha_list_entities → ha_get_state to check runtimes' },
        { icon: '🧮', text: 'Filters entities on for > 4 hours' },
        { icon: '⚡', text: 'ha_call_service to switch off + set scene' },
        { icon: '💬', text: 'Replies with what changed and total watt savings' },
      ],
    },
    {
      emoji: '🐍',
      tag: 'Code Sandbox',
      tagColor: 'orange',
      title: 'Run & iterate without leaving the chat',
      scenario: 'Paste a broken Python data pipeline. The agent runs it in an isolated sandbox, reads the traceback, fixes the bug, re-runs to confirm it passes, then shows you the final working code.',
      steps: [
        { icon: '▶️', text: 'execute_code runs the failing script in isolation' },
        { icon: '🔍', text: 'Reads stderr traceback — no API keys visible to sandbox' },
        { icon: '🔧', text: 'Patches the bug with write_file, re-runs to confirm' },
        { icon: '✅', text: 'Returns clean, tested, working code' },
      ],
    },
  ]}
/>

<QuickStart
  title="Five Ways to Install — Pick Yours"
  subtitle="npm, pip, cargo, Docker, and native binaries are verified against the current release. Homebrew support exists, but the external tap can lag until it is synced."
  tabs={[
    {
      label: 'npm',
      lang: 'bash',
      code: `# Install globally via npm — no Rust required
npm install -g edgecrab-cli

# Run setup wizard (detects API keys, writes config)
edgecrab setup

# Verify health (checks keys, provider ping, binary size)
edgecrab doctor

# Start chatting
edgecrab
edgecrab "summarise the git log for today"
edgecrab --model openai/gpt-5 "review this codebase"`,
    },
    {
      label: 'pip',
      lang: 'bash',
      code: `# Install via pip — no Rust required
# Use pipx for an isolated install (recommended)
pipx install edgecrab-cli
# or: python -m pip install --upgrade edgecrab-cli

# Run setup wizard
edgecrab setup

# Verify health
edgecrab doctor

# Start chatting
edgecrab
edgecrab "count lines in src/**/*.rs"
edgecrab --model ollama/llama3.3 "work completely offline"`,
    },
    {
      label: 'Cargo',
      lang: 'bash',
      code: `# Compile and install from crates.io (requires Rust 1.86+)
cargo install edgecrab-cli

# Run setup wizard
edgecrab setup

# Verify health
edgecrab doctor

# Start chatting
edgecrab
edgecrab "review the architecture docs"
edgecrab --model openai/gpt-5 "design a release checklist"`,
    },
    {
      label: 'Homebrew (macOS)',
      lang: 'bash',
      code: `# Homebrew support exists, but verify the tap is current
brew tap raphaelmansuy/tap
brew install edgecrab
edgecrab --version

# Run setup wizard
edgecrab setup

# Verify health
edgecrab doctor

# If brew still serves an older release, use npm, pip, cargo,
# Docker, or the native GitHub Release binaries until the tap sync completes.

# Start chatting
edgecrab
edgecrab "explain this codebase" --model openai/gpt-4o
edgecrab -w "explore that risky refactor" # isolated git worktree
edgecrab -S git-workflow "review this PR" # preload a skill`,
    },
    {
      label: 'Python SDK',
      lang: 'python',
      code: `pip install edgecrab

from edgecrab import Agent

# Simple chat
agent = Agent(model="openai/gpt-4o")
reply = agent.chat("Explain Rust ownership in 3 sentences")
print(reply)

# Async + streaming
import asyncio
from edgecrab import AsyncAgent

async def main():
    agent = AsyncAgent(model="copilot/gpt-5-mini")
    async for token in agent.stream("Write a Rust hello-world"):
        print(token, end="", flush=True)

asyncio.run(main())`,
    },
    {
      label: 'Node SDK',
      lang: 'typescript',
      code: `npm install edgecrab

import { Agent } from "edgecrab";

// Simple chat
const agent = new Agent({ model: "openai/gpt-4o" });
const reply = await agent.chat("Review this TypeScript code");
console.log(reply);

// Streaming
for await (const token of agent.stream("Write a README")) {
  process.stdout.write(token);
}

// npx CLI
// npx edgecrab-sdk chat "Hello!"
// npx edgecrab-sdk models`,
    },
    {
      label: 'Docker',
      lang: 'bash',
      code: `# Pull pre-built multi-arch image (linux/amd64 + linux/arm64)
docker pull ghcr.io/raphaelmansuy/edgecrab:latest

# Run gateway server (15 messaging gateways)
docker run -p 8642:8642 \\
  -e ANTHROPIC_API_KEY=sk-ant-... \\
  -e TELEGRAM_BOT_TOKEN=123456:... \\
  -v "$HOME/.edgecrab:/root/.edgecrab" \\
  ghcr.io/raphaelmansuy/edgecrab:latest

# Or with docker-compose (full gateway stack)
docker compose up -d
docker compose logs -f`,
    },
  ]}
/>

<ComparisonSection
  title="The DNA of EdgeCrab"
  subtitle="EdgeCrab is not a fork of either project. It absorbs what made each great — Hermes Agent's soul of autonomous learning and OpenClaw's messaging vision — then reforges it in Rust as a single binary that runs anywhere."
  eyebrow="Inspired By"
  columns={[
    { label: 'Hermes Agent 🧠' },
    { label: 'OpenClaw 🦞' },
    { label: 'EdgeCrab 🦀', highlight: true },
  ]}
  rows={[
    {
      feature: 'What it is',
      values: [
        'Autonomous agent (Python) — 40+ tools, learns and grows',
        'Personal assistant platform (TypeScript) — 24+ channels',
        `✓ SuperAgent — ${coreToolCount} core tools + ${gatewayCount} gateways + learns autonomously (Rust)`,
      ],
      highlight: true,
    },
    {
      feature: 'Core soul',
      values: [
        '✓ Lives on your server, remembers what it learns, improves over time',
        '✓ Always-on presence in your channels and tools',
        `✓ Both: Hermes soul (memory + learning) + OpenClaw presence (${gatewayCount} gateways)`,
      ],
    },
    {
      feature: 'User-first alignment',
      values: [
        '✓ Hermes LLM — steerable & user-aligned; Honcho cross-session user model',
        'Partial — depends on system prompt',
        '✓ Compiled-in Honcho user model — deepens with every session',
      ],
      highlight: true,
    },
    {
      feature: 'Deep reasoning',
      values: [
        '✓ Multi-turn + subagent delegation + parallel batch workers',
        'Via LLM API calls only',
        '✓ ReAct loop (90 iterations) + MoA (4 frontier models in parallel)',
      ],
    },
    {
      feature: 'Coding agent',
      values: [
        '✓ Terminal, file, browser + execute_code (Docker / SSH / Modal / Singularity)',
        'Partial — basic shell/git tools',
        '✓ ReAct + file + terminal + browser + execute_code (6 backends)',
      ],
      highlight: true,
    },
    {
      feature: 'Memory & learning',
      values: [
        '✓ Persistent memory + auto-skills + Honcho cross-session model',
        'Partial — in-session memory only',
        '✓ MEMORY.md + SQLite FTS5 sessions + Honcho + auto learning reflection',
      ],
    },
    {
      feature: 'Always-on presence',
      values: [
        '✓ 7 platforms: Telegram, Discord, Slack, WhatsApp, Signal, Email, CLI',
        '✓ 24+ channels: WhatsApp, Telegram, Slack, Discord, Matrix, IRC…',
        `✓ ${gatewayCount} gateways: Telegram, Discord, Slack, WhatsApp, Signal, Matrix, Mattermost, DingTalk, Email, SMS, HA, Webhook, API, Feishu, WeCom`,
      ],
      highlight: true,
    },
    {
      feature: 'Sub-agents',
      values: [
        '✓ Subagent delegation + parallel batch workers',
        '✗ Not supported',
        '✓ Up to 3 parallel sub-agents, depth limited to 2 levels',
      ],
    },
    {
      feature: 'Smart home',
      values: [
        '✗ Not supported',
        '✗ Not supported',
        '✓ Home Assistant — states, call services, trigger automations, history',
      ],
      highlight: true,
    },
    {
      feature: 'MCP support',
      values: [
        '✓ Full MCP client (mcp_tool.py, ~1050 lines)',
        '✗ Not supported',
        '✓ Built-in MCP client — JSON-RPC 2.0, stdio + HTTP + SSE transports',
      ],
    },
    {
      feature: 'Install method',
      values: [
        'curl | bash (Python + uv managed)',
        'npm install (Node.js runtime required)',
        '✓ npm · pip · cargo · Docker — pick yours',
      ],
      highlight: true,
    },
    {
      feature: 'Runtime footprint',
      values: [
        'Python runtime · uv · ~80–150 MB RAM',
        'Node.js runtime · ~80–200 MB RAM',
        '✓ Single Rust binary · no Python or Node runtime deps',
      ],
    },
    {
      feature: 'Cold startup',
      values: [
        '~1–3 s (Python + uv)',
        '> 500 ms on 0.8 GHz edge hardware',
        '✓ Single Rust binary with no Python or Node runtime dependencies for the agent itself',
      ],
      highlight: true,
    },
    {
      feature: 'Security',
      values: [
        '✓ 5 sandboxing backends (Docker, SSH, Modal, Singularity, local)',
        'Runtime patches',
        '✓ 7 compiled-in layers (type-level path jail + SSRF + cmd scan + sandbox + more)',
      ],
    },
    {
      feature: 'LLM providers',
      values: [
        '11 cloud providers',
        'Via OpenAI-compatible APIs',
        `✓ ${providerCount} built-in: Copilot, OpenAI, Anthropic, Gemini, Vertex AI, Bedrock, xAI, DeepSeek, Mistral, Groq, HuggingFace, Z.AI, OpenRouter, Ollama, LM Studio`,
      ],
      highlight: true,
    },
  ]}
/>

<CtaSection
  title="From the Soul of Hermes to the Speed of Rust"
  subtitle="EdgeCrab is open source, Apache 2.0. Your SuperAgent runs on macOS, Linux, Windows, and edge hardware. Use npm, pip, cargo, Docker, or native release binaries today; use Homebrew once the external tap has been synced."
  installCmd="npm install -g edgecrab-cli"
  primaryHref="/getting-started/quick-start/"
  primaryText="Get Started Free"
  secondaryHref="https://github.com/raphaelmansuy/edgecrab"
  secondaryText="Star on GitHub"
/>
