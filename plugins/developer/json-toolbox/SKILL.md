---
name: json-toolbox-skill
description: Use JSON validation and JSON Pointer lookup tools for exact payload inspection.
compatibility: Requires json-toolbox plugin
metadata:
  hermes:
    related_skills: [api-debugging]
---

# JSON Toolbox Skill

Use `json_validate` before editing or forwarding machine-readable payloads.
Use `json_pointer_get` when the user asks for a precise field buried inside JSON.
